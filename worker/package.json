{
  "name": "khdp-ai-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker fronting khdp.ai — proxies /AGENTS.md to GitHub raw and /v1/* to the KHDP backend.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260530.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "wrangler": "^4.0.0"
  }
}
